plugins {
    id 'java'
}

group 'com.playernguyen'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'

    compileOnly files('lib/server.jar')
    compileOnly files('lib/weaponist.jar')
}
